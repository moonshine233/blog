spring:
  datasource:
    url: jdbc:mysql:///blog?useUnicode=true&characterEncoding=utf-8
    username: root
    password: 1402285087
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
logging:
  level:
    root: warn
    com.hwd: info
  file: log/blog-dev.log

server:
  port: 80